export class Day {
    extraMinutes: number;

    // type enum
    constructor(
        public type: string,
        public year: number,
        public month: number,
        public day: number
    ) {}
}
